backend/
├── Dockerfile
├── .env
├── src/
│   ├── main.js						# Point d'entrée principal (Le point d’entrée de l’appli. Lance Fastify et appelle app.ts)
│   ├── app.js						# Initialisation de Fastify, plugins, routes (Configure Fastify : plugins, middlewares, routes.)
│   │ 								# C’est ici qu’on "branche" tous les modules.
│
│   ├── config/                       # Configurations (CORS, WAF, Vault, etc.)
│   │   ├── index.js					
│   │   ├── env.config.js				# gestion des variables .env
│   │   ├── security.config.js			# règles CORS, CSRF, etc.
│   │   ├── vault.config.js				# configuration de Vault
│   │   └── oauth.config.js				# configuration OAuth 42
│
│   ├── modules/                      # Chaque fonctionnalité principale
│   │   ├── auth/                     # Authentification (OAuth, JWT, 2FA)
│   │   │   ├── auth.controller.js		# routes d’auth (login, callback OAuth, 2FA...)
│   │   │   ├── auth.service.js			# logique de vérification (token, 2FA, validation OAuth...)
│   │   │   ├── auth.routes.js			
│   │   │   ├── oauth.strategy.js		# stratégie OAuth (avec 42 par ex)
│   │   │   ├── jwt.strategy.js			# validation des tokens JWT
│   │   │   └── 2fa.service.js			# gestion du 2FA (QR code, OTP...)
│   │   │
│   │   ├── users/                    # Gestion des utilisateurs
│   │   │   ├── user.controller.js		# routes liées aux users (profil, update, etc.)
│   │   │   ├── user.service.js			# logique métier (update profile, fetch user, block user...)
│   │   │   ├── user.routes.js
│   │   │   └── user.entity.js			# représentation TypeScript ou ORM du User (modèle)
│   │   │
│   │   ├── game/                     # Module Pong
│   │   │   ├── game.gateway.js			# WebSocket (optionnel si partie en ligne pour sunchro) 
│   │   │   │							# (envoi de position des raquettes, balles, etc.)
│   │   │   ├── game.service.js			# Matchmacking
│   │   │   ├── game.engine.js			# Moteur du jeu
│   │   │   ├── game.manager.js			# Gere plusieurs parties simultane
│   │   │   └── game.controller.js		# Endpoints HTTP liés au jeu ( creation des parties, matchmaking, historique, etc.) (par exemple,
│   │   │								# rejoindre une partie, voir ses stats, relancer une partie, etc.)
│   │   │
│   │   ├── chat/                     # Module Chat en temps réel
│   │   │   ├── chat.gateway.js			# WebSocket pour chat
│   │   │   ├── chat.service.js			# logique (messages, salons, modération...)
│   │   │   └── chat.controller.js		# endpoints HTTP (historique, création de salon...)
│   │   │
│   │   ├── security/                 # GDPR, suppression, anonymisation, WAF
│   │   │   ├── gdpr.service.js			# suppression des données, anonymisation
│   │   │   ├── waf.middleware.js		# Si WAF implémenté côté code : "pare-feu" applicatif (protection basique côté code)
│   │   │   └── account-deletion.js		# suppression compte utilisateur
│   │
│   ├── plugins/                      # Plugins Fastify (JWT, WebSocket, CORS, etc.)
│   │   ├── jwt.plugin.js				# plugin JWT pour sécuriser les routes
│   │   ├── websocket.plugin.js			# plugin pour gérer WebSocket avec Fastify
│   │   ├── vault.plugin.js				# si tu utilises HashiCorp Vault ou similaire
│   │   └── logger.plugin.js			# logger custom (console ou fichier)
│
│   ├── middlewares/                 # Middlewares personnalisés
│   │   ├── rate-limit.middleware.js	# limiter les requêtes (anti-spam)
│   │   ├── csrf.middleware.js			# protection contre les attaques CSRF
│   │   └── auth-check.middleware.js	# vérifie si l’utilisateur est authentifié
│
│   ├── utils/                        # Fonctions utilitaires (hashing, validation, etc.)
│   │   ├── hash.util.js				# hash de mots de passe ou données sensibles
│   │   └── validation.util.js			# validateurs custom (emails, formats, etc.)
│
│   ├── types/                        # Déclarations de types et interfaces
│   │   └── user.type.js				# types User (DTO, interfaces...)
│
│   └── database/                     # ORM ou accès à la DB
│       ├── index.js					# init de la DB (Prisma ou autre)
│       ├── prisma/						# Si tu utilises Prisma, par ex.
│       │   ├── schema.prisma				# le schéma de ta base de données
│       │   └── client.js					# le client Prisma généré
│       └── migrations/					# migration SQL générée par Prisma

